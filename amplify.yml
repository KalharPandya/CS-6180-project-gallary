version: 1
frontend:
  phases:
    preBuild:
      commands:
        - git lfs install
        - git lfs pull
        - node scripts/build.js
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []
